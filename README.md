# ab-sboot
seed spring boot

```
./mvnw clean install
./mvnw clean package
./mvnw test
./mvnw compile
./mvnw spring-boot:run
```

Endpoint `http://localhost:8080/product` or `http://localhost:8080/product?id=123` 
